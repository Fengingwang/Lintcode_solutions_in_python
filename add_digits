# digit root problem 
# dr(n) = 1 + (n-1) mod 9

class Solution:
    """
    @param num: a non-negative integer
    @return: one digit
    """
    def addDigits(self, num):
        # write your code here
        if num == 0: return 0
        return 1 + (num - 1) % 9
        
